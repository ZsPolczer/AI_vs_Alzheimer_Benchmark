"""Runnable entry points for the EATEOT lab.

`lab`, `compare`, and `plot` expose ``main()`` wired to console scripts in
``pyproject.toml`` (``eateot-lab``, ``eateot-compare``, ``eateot-plot``).
`dashboard` is a Streamlit app, run via ``streamlit run apps/dashboard.py``.
"""
