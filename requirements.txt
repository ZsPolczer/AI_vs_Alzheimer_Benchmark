# ============================================================
# EATEOT Neural Degradation Lab — Python dependencies
# ============================================================
# NOTE: PyTorch with CUDA must be installed from the PyTorch index:
#
#   uv pip install --python .venv/bin/python torch \
#       --index-url https://download.pytorch.org/whl/cu128
#
# (or `uv pip install torch` from PyPI if you don't need GPU support)

# --- Core ML / model loading ---
# Verified working: transformers 5.14.1 (smoke-tested model load, layer access, generation)
# <6 blocks the unverified 6.x major line
# torch is intentionally NOT listed here — install it first from the CUDA index (see README)
transformers>=4.44,<6
safetensors
accelerate

# --- Dashboard (dashboard.py) ---
streamlit>=1.37
pandas>=2.2,<3
plotly>=5.22

# --- Plotting (plot_decay.py) ---
matplotlib>=3.8
